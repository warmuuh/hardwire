package root;

import javax.inject.Inject;
import lombok.Data;
import org.springframework.stereotype.Component;
import java.util.ArrayList;



public class ContainerFactory {
	
	Object[] classes = new Object[{{classes.size}}];

	public void start(){
		{{#classes}}
		classes[{{id}}] = new {{packageName}}.{{name}}();
		{{/classes}}
		
		//wire up everything:
		
		{{#classes}}
		{{#fields}}
		(({{packageName}}.{{name}})classes[{{id}}]).set{{setterName}}(({{type.packageName}}.{{type.name}})classes[{{type.id}}]);
		{{/fields}}
		{{/classes}}
	}	


}
